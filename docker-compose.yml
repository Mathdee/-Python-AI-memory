version: '3.8'

services:
  brain:
    build: .
    container_name: metabolic-brain
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data  # Persists the SQLite db if you enable it
    restart: unless-stopped
    environment:
      - PORT=8000